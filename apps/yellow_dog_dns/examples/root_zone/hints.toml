# Root Zone Configuration - Hints Strategy
#
# This strategy uses embedded root server addresses.
# No external fetching or zone files required.
# Recommended for most recursive resolver deployments.

[dns.root_zone]
strategy = "hints"

# No additional configuration required for hints strategy.
# The module uses hardcoded root server addresses (a-m.root-servers.net)
# that are updated with each release.
